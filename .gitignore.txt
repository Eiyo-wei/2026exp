# 1. 忽略所有文件和文件夹
*

# 2. 只允许这三个文件夹进入版本库
!service-provider-8001/
!service-provider-8002/
!service-provider-8003/

# 3. 在这三个文件夹内，只允许 .java 和 .yml 文件
!service-provider-8001/**/*.java
!service-provider-8001/**/*.yml
!service-provider-8002/**/*.java
!service-provider-8002/**/*.yml
!service-provider-8003/**/*.java
!service-provider-8003/**/*.yml

# 4. 仍然忽略常见的无关文件（可选，但保持干净）
*.class
target/
*.log
.DS_Store