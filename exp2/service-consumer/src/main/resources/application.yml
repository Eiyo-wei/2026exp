spring:
  application:
    name: service-consumer
  cloud:
    discovery:
      client:
        simple:
          instances:
            service-provider:
              - uri: http://localhost:8001
              - uri: http://localhost:8002
              - uri: http://localhost:8003
            custom-provider:
              - uri: http://localhost:8001
              - uri: http://localhost:8002
              - uri: http://localhost:8003

server:
  port: 8080

lab2:
  loadbalancer:
    strategy: random
